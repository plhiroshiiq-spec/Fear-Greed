SPEC.md
# FG ENGINE 仕様書 v2.2 — CNN Fear & Greed → SUMI DECK(米国版のみ・運用費0円)

作成: 2026-09-18 / 対象実装者: Claude Code / 発注者: ちゃなぱぱ

この文書は Claude Code がそのまま読んで実装を進めるための仕様書である。
リポジトリ直下に `SPEC.md` として置き、11章のキックオフ指示文を貼って開始する。

## 変更総括(常に最新を冒頭に置くこと)

### v2.2 (2026-09-18) — 推移線に自動描画のライン(支持/抵抗/下降線)を追加
| 項目 | v2.1(変更前) | v2.2(変更後) |
|---|---|---|
| 推移線の本数 | 18営業日 | **60営業日**(スイング点の検出に必要なため) |
| 補助線 | なし | **水平の支持線・抵抗線を自動描画**(5.3章)。下降トレンドラインは任意で追加 |
| 色 | 全面モノクロ | **自動描画の線だけ色を使う**(紫/オレンジ/緑)。推移線と数値は白のまま |
| 設定 | — | 補助線を「水平線のみ / 水平線+斜線 / 線なし」の3段階で切替。**既定は水平線のみ** |

**色を部分的に入れた理由**: モノクロ画面で細い線を重ねると判別できないため。色を自動描画の線に限定することで「白=実データ、色=プログラムの解釈」という区別が一目でつく。

**斜線を既定にしない理由**: 検討の結果、斜線(トレンドライン)には次の弱点が確認された。付録Cに検証の経緯を残す。

### v2.1 (2026-09-18) — 表示デザインを案Eに確定。既定を常時表示に変更
| 項目 | v2.0(変更前) | v2.1(変更後) |
|---|---|---|
| ゲージの形 | 半円ゲージ(CNN踏襲)と記載 | **案E「帯と推移」に確定**(5章に実装寸法を明記)。半円ゲージは不採用 |
| 折りたたみの既定 | 条件付き折りたたみがON | **常時表示がON**。折りたたみは設定で選択制に降格 |
| 履歴の用途 | スパークラインは展開時のみ | **推移線を常時表示**。案Eの中核要素のため |

**既定を変えた理由**: 発注者の目的が「毎日見て意識づけする」ことだと判明したため。常設枠がUSゲージ1枚だけで画面が混まない以上、平常時に隠すのは目的と逆行する。折りたたみ機能自体は残し、使ってみて飽きたら設定で切り替えられるようにする。

### v2.0 (2026-09-18) — 米国版のみに絞り込み。運用費を0円化
**方針転換の理由**: 日本版(JFG)の自前算出には J-Quants Standard(月額3300円)が必要で、費用が見合わないと判断。CNNのデータは**APIキー不要・契約不要**で取得できるため、米国版だけなら運用費が完全に0円になる。

| 項目 | v1.1.1(変更前) | v2.0(変更後) |
|---|---|---|
| 対象 | 米国(CNN)+ 日本(JFG自前算出) | **米国(CNN)のみ** |
| 運用費 | 月3300円(J-Quants Standard) | **0円** |
| 外部契約 | J-Quants APIキーが必須 | **不要**(GitHubアカウントのみ) |
| フェーズ | P0〜P9(9段階) | **P1〜P5(5段階)**。JP関連のP0・P3・P4を削除 |
| 想定工数 | 数週間 | **1〜2日** |
| 常設枠 | USゲージ+JPゲージ+可変1枠 | **USゲージ + 可変1枠**(計2枠) |
| 第3枠 SL WATCH | J-Quantsの終値で判定 | **楽天証券CSV+手入力で判定**(0円のまま実装可、9章) |
| 日本版の設計 | 本文2章 | **付録Bに凍結保存**。将来復活させる際の手順も記載 |
| `fg.json` | schema 1(us/jp両方) | **schema 2**(us のみ。jpキーは予約だけして出力しない) |

**削除していないもの**: 1章のCNN解析結果(本プロジェクト最大の資産)、表示制御の規定、置かないものリスト。これらはv1.1.1から内容を維持している。

### v1.1.1 (2026-09-18) — リンク集を追加、HTML版のURLをタップ可能に
### v1.1 (2026-09-18) — 第3枠と表示制御を追加
### v1.0 (2026-09-18) — 初版(日本版を含む構成)

---

## 0. 目的と全体像

Galaxy Z Fold8 の自作ランチャー SUMI DECK のホーム画面に、CNN Fear & Greed Index のゲージを常時表示する。

```
[fg-engine] Python / GitHub Actions(public/private どちらでも可)
   └ CNN JSON 取得 → 検証 → data/fg.json
                               ▼
[SUMI DECK] Kotlin / Jetpack Compose
   fg.json を取得 → キャッシュ → モノクロのゲージを描画
```

設計原則(優先順)

1. **壊れても嘘をつかない**: 取得失敗時は前回値を保持し `stale` を立てる。推測値で埋めない。枠を黙って消さない。
2. **CNNの値をそのまま出す**: 加工・再計算はしない。CNNが出した数字を正確に運ぶことだけに責任を持つ。
3. **枠を増やさない**: 常設はUSゲージ1枚+可変1枠の**計2枠**。7章の除外リストを守る。
4. **0円を維持する**: 課金が必要な外部サービスを勝手に追加しない。

---

## 1. CNN解析結果 — 2026-09-18 に実エンドポイントで確認済みの事実

**本章は実測に基づく確定情報である。推測で書き換えないこと。**

### 1.1 取得先

- `GET https://production.dataviz.cnn.io/index/fearandgreed/graphdata`
- `GET https://production.dataviz.cnn.io/index/fearandgreed/graphdata/YYYY-MM-DD`(指定日〜現在の履歴)
- **公式APIではない**。CNNのページが内部で叩いているJSON。ブラウザ風の `User-Agent` と `Referer: https://www.cnn.com/markets/fear-and-greed` が無いと拒否される。
- 予告なく仕様変更・遮断される可能性がある。個人利用の範囲に留め、データの再配布はしない。遮断時の挙動は4章に従う。

### 1.2 JSON構造(実測)

```
fear_and_greed: { score, rating, timestamp, previous_close, previous_1_week, previous_1_month, previous_1_year }
fear_and_greed_historical: { timestamp, score, rating, data:[{x(ms,UTC), y(score), rating}, ...] }
market_momentum_sp500     : { timestamp, score, rating, data:[{x, y=S&P500終値}] }
market_momentum_sp125     : { …, data:[{x, y=S&P500の125日移動平均}] }
stock_price_strength      : { …, data:[{x, y=52週新高値−新安値の純比率(%)}] }
stock_price_breadth       : { …, data:[{x, y=McClellan Volume Summation Index}] }
put_call_options          : { …, data:[{x, y=プット/コール比の5日平均}] }
market_volatility_vix     : { …, data:[{x, y=VIX}] }
market_volatility_vix_50  : { …, data:[{x, y=VIXの50日移動平均}] }
junk_bond_demand          : { …, data:[{x, y=ジャンク債と投資適格債の利回り差}] }
safe_haven_demand         : { …, data:[{x, y=株と債券の20日リターン差}] }
```

要素は7つ(モメンタム/株価の強さ/市場の幅/プットコール/ボラティリティ/ジャンク債需要/安全資産需要)。`market_momentum_sp125` と `market_volatility_vix_50` は参照線であり独立要素ではない。

### 1.3 実測から確定した事項

| 項目 | 事実 | 実装上の指示 |
|---|---|---|
| 総合値 | **7要素スコアの単純平均**。2026-09-17: 29.6285…×7=207.4 で、確認できた5要素(26.4+3+0.2+36.8+50)と整合 | 自前で再計算せず `fear_and_greed.score` をそのまま使う |
| 値の精度 | 小数のfloatで配信(例 29.6285714…) | **表示の丸め規則はP1で複数日照合して確定**。CNN画面が29表示だったため切り捨ての可能性がある。確定するまでは四捨五入とし、判明後に統一する |
| 区分 | <25 Extreme Fear / 25–45 Fear / 45–55 Neutral / 55–75 Greed / ≥75 Extreme Greed | `rating` 文字列をそのまま使い、日本語表記に変換して表示 |
| 履歴末尾 | **最終行が当日の現在時刻で重複する** | 日付でdedupeする。これを怠るとスパークラインの右端が二重になる |
| 要素data内のrating | **信用できない**(全行に同じラベルが入っている) | 無視する。要素の区分が必要なら `score` から自前で判定する |
| 更新 | 米国市場時間中に随時。確定は米国引け後 | 4章のスケジュールに従う |

### 1.4 参考: 正規化方式についての観測(実装不要)

CNNは各要素の0–100化の式を公開していない。ただし実データから、**絶対水準ではなく直近の自分自身の分布に対する相対位置**で採点していることが読み取れる(例: S&P500が125日線を+3.6%上回っているのにモメンタムは26.4=Fear。1年前の乖離は+9.7%)。

この観測は日本版を復活させる場合にのみ必要となる(付録B)。**v2.0の実装では不要**。

---

## 2. 出力: `data/fg.json`(schema 2)

```json
{
  "schema": 2,
  "generated_at": "2026-09-18T06:41:12+09:00",
  "us": {
    "source": "cnn",
    "as_of": "2026-09-17",
    "score": 29.63,
    "rating": "fear",
    "prev_close": 26.49,
    "w1": 32.20,
    "m1": 59.14,
    "y1": 63.77,
    "delta": -2.34,
    "streak": -3,
    "components": [
      {"key": "momentum",    "name_ja": "モメンタム",   "score": 26.4, "raw": 3.61},
      {"key": "strength",    "name_ja": "株価の強さ",   "score": 3.0,  "raw": -3.40},
      {"key": "breadth",     "name_ja": "市場の幅",     "score": 0.2,  "raw": 638.17},
      {"key": "put_call",    "name_ja": "プット/コール", "score": 36.8, "raw": 0.7597},
      {"key": "volatility",  "name_ja": "ボラティリティ", "score": 50.0, "raw": 18.2},
      {"key": "junk_bond",   "name_ja": "ジャンク債需要", "score": 0,   "raw": 0},
      {"key": "safe_haven",  "name_ja": "安全資産需要",  "score": 0,   "raw": 0}
    ],
    "history": [["2025-09-17", 63.77]],
    "stale": false
  }
}
```

- `delta` = 当日 − 前日。`streak` = 同方向の連続日数(上昇は正、下降は負)。**どちらもClaude Code側で計算して入れる**(10.4章の折りたたみ表示に必須)。
- `history` は直近260営業日。日付でdedupe済みであること。
- `jp` キーは**予約のみ**。v2.0では出力しない。SUMI DECK側は `jp` が無くても正常動作すること。
- 併せて `data/history_us.csv`(date, score, 各要素score)を追記保存する。

---

## 3. 実装フェーズと検収条件

### P1 CNN取得 → fg.json → GitHub Actions 稼働
- 構成: `fg/cnn.py`(取得・リトライ・検証)、`fg/build_json.py`、`tests/`。Python 3.12 / `requests` / `pytest`。
- 丸め規則の確定: 3営業日ぶん、CNNサイトの表示値と `score` の小数を突き合わせ、四捨五入か切り捨てかを判定して `docs/rounding.md` に記録。
- 検収:
  - `fg.json.us.score` がCNNサイトの表示と一致
  - `history` に重複日が無い
  - `delta` と `streak` が手計算と一致
  - 7要素すべてに `score` が入っている
  - 取得失敗をモックして `stale:true` になり、**既存の fg.json が壊れない**ことをテストで確認

### P2 SUMI DECK 移植(5章)
- **採用デザインは案E「帯と推移」(5.1章)+自動描画ライン(5.3章)。** 半円ゲージは作らない。
- 検収は5章末尾に記載。推移線のY軸スケーリングと、履歴が18本未満のときの挙動もテストすること。

### P3 表示制御(6章)
- 手動オン/オフ → 条件付き折りたたみ の順で実装。

### P4 SL WATCH(9章)
- P1〜P3が安定してから着手。

### P5(任意)EXT / 通知
- 外部環境の表示(9.2)、区分が変わった日だけの通知。

**P1の完了時に必ず報告し、発注者の確認を待ってからP2へ進むこと。**

---

## 4. 運用スケジュール(GitHub Actions / cron はUTC。遅延10〜30分を許容)

| ジョブ | JST | 内容 |
|---|---|---|
| `morning` | 火〜土 06:40 | 米国引け後の確定値を取得 |
| `intraday` | 火〜土 00:30 | 米国市場時間中の値を1回更新(任意。寄り前に最新化したい場合) |

失敗時の挙動(**厳守**):

1. リトライ(指数バックオフ3回)
2. なお失敗 → 日付付きURL(`graphdata/YYYY-MM-DD`)にフォールバック
3. それでも失敗 → **既存の fg.json を上書きせず**、`stale:true` だけを立てて終了コード0で終わる
4. 3回連続で失敗した場合はワークフローを失敗させ、GitHubからメール通知が飛ぶようにする

取得したJSONの妥当性検証(1つでも外れたら採用しない):
- `score` が 0〜100 の範囲
- `rating` が既知の5種類のいずれか
- `history` の最新日が前回保存分から**後退していない**

---

## 5. SUMI DECK 側仕様(Kotlin / Jetpack Compose)

**最初に既存コードベースを読み、DI・テーマ・画面構成・命名の流儀に合わせること。** 以下は最小の契約であり、既存の流儀と衝突する場合は既存側を優先する。

- 取得: リポジトリの `data/fg.json` を取得。**publicリポジトリなら raw URL を直接叩けば認証不要**(0円方針と相性が良いのでこちらを推奨。CNNのデータを再配布しない配慮として、リポジトリにはスコアのみを置き生JSONは保存しない)。privateにする場合は GitHub Contents API + 読み取り専用 fine-grained PAT を `local.properties` → `BuildConfig` で注入(**リポジトリにコミットしない**)。
- 更新: ランチャー復帰時に最終取得から30分以上経過していれば再取得。加えて WorkManager で 07:00 JST 前後に1回。
- キャッシュ: 最後に成功したJSONを DataStore に保持。通信失敗時はキャッシュを表示し、`generated_at` が36時間以上前 or `stale:true` なら「更新遅延」を小さく表示。
- モデル: `kotlinx.serialization`。`ignoreUnknownKeys = true`。`schema` が想定より大きくてもクラッシュしないこと。`jp` キーが**無い**ことを前提にする(将来追加されても落ちないこと)。
### 5.1 採用デザイン: 案E「帯と推移」(確定)

半円ゲージは**不採用**。CNNの意匠に似すぎることと、推移が見えないことが理由。採用する構成は上から順に「数値 → 位置の帯 → 推移線」の3段。

`FearGreedPanel(score, rating, delta, streak, history, stale)` を1つ描く。基準幅334dp(画面幅390 − 左右パディング28)で、各段の寸法は次の通り。

**1段目 数値行**
- スコア: 46sp / Light。区分名: 16sp、`--text-secondary`相当。右端に `delta` と `streak` を14spで `−2 ↓3日` の形。
- 区分の日本語表記: 極度の恐怖 / 恐怖 / 中立 / 強欲 / 極度の強欲

**2段目 位置の帯**(数値行の16dp下)
- 高さ6dpの帯を5区分に分割。幅は区分の実幅に比例(0–25 / 25–45 / 45–55 / 55–75 / 75–100)。区分間に2dpの隙間。
- 現在位置マーカー: 幅3dp・高さ18dp、帯の上下にはみ出す。色は前景色(白)。**帯そのものは全区分同じ濃度**にし、色ではなくマーカー位置だけで現在地を示す(モノクロのため)。
- 帯の下に 0 / 25 / 45 / 55 / 75 / 100 の目盛り数字を11sp、`--text-muted`相当で両端揃え。

**3段目 推移線**(帯の26dp下)
- 直近**60営業日**の `history` を折れ線で描く。高さ88dp。線幅1.8dp、色は前景色(白)。線の結合は丸め。
- Y軸は固定ではなく、**表示区間(推移線と描画中の補助線を含む)の最小値−4 と 最大値+4 でスケーリング**する(固定0–100だと平常時に線が寝て動きが読めないため)。
- 最新点にだけ半径3.8dpの点を打ち、前景色で塗る。
- 線の下に「過去60営業日」を11spで添える。
- `history` が60本に満たない場合は、ある分だけ描く。**足りない分を0や前値で埋めない。**
- 上に重ねる補助線は5.3章。

**4段目(展開時のみ)**
- 7要素の横棒、前日/1週/1か月/1年の4値。

### 5.3 自動描画ライン(支持・抵抗・下降線)

推移線の上に補助線を重ねる。**設定で3段階**: `水平線のみ`(既定) / `水平線+斜線` / `線なし`。

#### 5.3.1 スイング点の検出
- **スイング安値**: 前後1本より値が小さい(以下)点。**スイング高値**: 前後1本より値が大きい(以上)点。
- 左右1本で判定する。3本にすると直近の小さな山を拾えず、線が古い高値に引かれて実態と合わなくなることを実データで確認済み。
- 当日と前日は**確定していない**ため、スイング点として採用しない(右側1本が未確定のため)。

#### 5.3.2 水平線(既定でON。2本)
- **支持線** = 直近の確定スイング安値の値。**抵抗線** = 直近の確定スイング高値の値。
- 起点より左は同色・線幅2dp・不透明度28%で薄く引き、起点から右端までは**線幅3dp・破線(7dp描画/4dp空白・端は丸め)**で引く。どこから有効な線かが分かるようにするため。
- 起点に半径3.2dpの円マーカー(線と同色、中は背景色で抜く)。
- 色: 抵抗=オレンジ系、支持=緑系。
- **スイング点が1つも無い期間は線を引かない。** 直近値や期間の最大最小で代用しない。

#### 5.3.3 下降/上昇ライン(斜線。既定でOFF)
- 直近3つの確定スイング高値に**最小二乗で直線を当てはめ**、右端まで延長する(下降線)。上昇線は安値3点で同様。
- 3点が揃わない日は**線を描かない**。
- 色: 紫系。線幅3dp、実線。
- 終値が線を上抜けた(下降線)/下抜けた(上昇線)日に、その点に半径5.5dpの円マーカーを線と同色で打つ。マーカーは3営業日残す。
- **無効化の条件**(いずれかに該当したら線を描かない):
  - 傾きの絶対値が1日あたり1.5ポイントを超える(急落局面で線が立ちすぎ、数日で0や100を割るため)
  - 3点目から10営業日を超えて経過している(古い線を延々と延ばさない)
  - 下降線と上昇線を同時に描く設定で、2本が表示区間内で**交差する**(上下の意味が入れ替わり表示が破綻するため、両方消す)

#### 5.3.4 凡例
推移線の下に12spで置く。描画中の線だけを出す。

```
— 下降線 上抜け   — 抵抗 32.7   — 支持 27.3
```

#### 5.3.5 色の扱い(重要)
**色を使うのは自動描画の線と凡例だけ。** 推移線・数値・区分名・位置の帯は白と灰のまま。これにより「白=実際のデータ、色=プログラムが引いた解釈」という区別が成立する。区分の強弱を色で表すことは引き続き禁止(5.2章)。

### 5.2 その他のUI規定
- **モノクロ**: 区分・強弱を色で表現しない。濃度・位置・テキストで表す。色の例外は5.3.5のみ。
- レイアウト: 閉じた状態(カバー画面)=案Eのパネル+可変枠。開いた状態=案E+7要素の横棒。
- 検収: 機内モードで起動してもキャッシュ表示される / `score` が null でもクラッシュしない / 折りたたみ開閉で再コンポーズが破綻しない / 折りたたみ時に数値が読める(6.3章) / スイング点が無い期間に線が引かれない / 斜線の無効化条件3つが効く。

---

## 6. 表示制御

### 6.1 手動オン/オフ(必須・最優先)
設定画面で `USゲージ` / `可変枠` を個別に表示切替。DataStore に保存。
**完全非表示はこの手動オフの時だけ。** それ以外の理由で枠が画面から消えてはならない。

### 6.2 条件付き折りたたみ(**既定でOFF**)
**既定は常時表示。** 目的が毎日見て意識づけすることなので、平常時に隠さない。

折りたたみ機能は設定で選べるようにだけしておく(使い続けて情報が飽和したときの逃げ道)。ONにした場合の展開条件:

| 枠 | 展開する条件 | 平常時 |
|---|---|---|
| USゲージ | score < 25 または > 75、`delta` の絶対値 ≥ 5、`streak` の絶対値 ≥ 3 | 折りたたみ |
| SL WATCH | 割れ ≥ 1件、または最短距離 < 2%(**こちらは既定でON**。割れ0件の日に枠を出す意味がないため) | 折りたたみ |

### 6.3 折りたたみ時の表示(**この節は必ず守ること**)

**折りたたみ = バーだけにする、ではない。数値は常に残す。**
バーや帯だけでは51なのか54なのかが読めず、5秒判断の役に立たない。畳むのはゲージの円弧・目盛り・要素内訳・スパークラインであって、数値ではない。

折りたたみ時の1行に必ず含めるもの:

```
US  29  恐怖   −2 ↓3日
└名前└数値 └区分  └前日差 └連続日数
```

- **数値**(整数)…必須
- **区分名**…必須(モノクロ画面では色で区分を伝えられないため)
- **前日差と連続日数**…必須(センチメントは水準より方向の持続で効く)
- 細い水平バーは**補助**として添えてよいが、数値の代わりにはしない
- 案Eを畳む場合、落とすのは推移線・補助線・目盛り数字。**数値行と位置の帯は残す**

### 6.4 消えてはいけないケース
- **取得失敗・stale・データ欠損のときに枠を消すことを禁止する。** 「平常だから畳んだ」のか「データが来ていない」のかが区別できなくなるため。畳んだ1行を残し、数値の代わりに `— 更新遅延(09/16)` と最終取得日を出す。
- 推測値や前日値を現在値として出さない。

---

## 7. 画面に置かないもの(意図的な除外)

枠を増やさないための判断であり、実装漏れではない。**Claude Code はこれらを善意で追加しないこと。**

| 置かないもの | 理由 |
|---|---|
| VIX、プット/コール等の個別要素の単独表示 | 詳細シート内にのみ置く。ホーム画面には出さない |
| 個別銘柄の株価・チャート | TradingViewのアラートの役割 |
| ニュース、ヒートマップ、ランキング | 見た瞬間に時間を溶かす。1銘柄5秒の運用と相反する |
| 日本市場の指標(騰落レシオ等) | v2.0の対象外。復活させる場合は付録Bの手順で |

将来追加したくなった場合は、**既存の枠を1つ削ってから**追加する(計2枠の上限を動かさない)。

---

## 8. Claude Code の進め方の決まり

- フェーズは順番に。各フェーズの最後に検収条件の結果を報告し、**P1は発注者の確認を待ってからP2へ**。
- 仕様と実際のAPIが食い違ったら、黙って合わせず `docs/spec_diff.md` に差分を書いてから進む。
- **課金が必要な外部サービスを勝手に追加しない。** 必要だと判断した場合は実装せず、理由と代替案を報告する。
- CNNの生データをリポジトリに保存・再配布しない。保存するのはスコアと派生統計のみ。

---

## 9. 第3枠(可変スロット)

常設はUSゲージ1枚。可変枠は**同時に1つだけ**表示する。

### 9.1 SL WATCH(本命・0円で実装可)

保有建玉のうち、直近終値がSLラインを割っている銘柄数を出す。**J-Quantsを使わずに実装できる。**

**前提**: 発注者のSL運用は終値判定。ザラ場中のリアルタイム株価は取得しない(TradingViewのアラートの役割)。判定は引け後に行い、翌朝まで表示する。

- 入力(手動): `code` / `entry`(建値) / `sl` / `shares` / `opened_at`。アプリ内フォームから追加(1件20秒程度)。
- 終値の入手(0円の選択肢。P4で選定し `docs/price_source.md` に記録):
  - (a) 引け後に手動で終値を入力(建玉が数銘柄なら現実的)
  - (b) 無料の株価ソースを1本だけ追加(**株式分割の調整が信用できないソースは使わない**。分割で新高値判定やSL距離が静かに壊れるため、分割イベントの扱いを必ず確認すること)
  - 判断がつかない場合は(a)で実装し、(b)は保留する
- SLの自動更新: `sl_rule` があれば再計算(`none` / `breakeven` / `atr` / `swing`)。既定は `none`。**SLを自動で下げることは絶対にしない。**
- 決済の消し込み: 楽天証券の実現損益CSV(Shift-JIS)を取り込めるなら自動クローズ。取り込めない場合は手動クローズ。
- 保存先: `positions.json`(**端末内 DataStore のみ**。リポジトリには置かない)。
- 判定: `close < sl` を「割れ」。`dist_pct = (close − sl) / close × 100` の最小値も出す。
- 表示: `SL 建玉5 / 割れ1 (7203) 最短 +1.2% 09/17終値`。判定に使った終値の日付を必ず添える。

### 9.2 EXT(任意・P5)

寄り前の外部環境を1つだけ。既定はSOX指数の前日比%。データ源は無料のものをP5で選定し、取得失敗時は枠ごと非表示にする(誤った値は絶対に出さない)。

---

## 10. 発注者(人間)側の準備

1. GitHub に リポジトリ `fg-engine` を作成(publicでもprivateでも可。5章の取得方式に影響する)
2. 以上。**APIキーの取得も課金も不要。**

---

## 付録A リンク集

実際に到達を確認したURLのみ記載。

### 最初に開くもの
- GitHub 新規リポジトリ作成: https://github.com/new

### CNN
- 公式ページ(Refererに使う): https://www.cnn.com/markets/fear-and-greed
- JSONエンドポイント: https://production.dataviz.cnn.io/index/fearandgreed/graphdata
- 日付指定(履歴): https://production.dataviz.cnn.io/index/fearandgreed/graphdata/2020-09-01

### 日本版を復活させる場合のみ(付録B)
- J-Quants トップ / プラン契約: https://jpx-jquants.com/ja
- プラン別の利用可否とデータ期間: https://jpx-jquants.com/ja/spec/data-spec
- 更新タイミング一覧: https://jpx-jquants.com/ja/spec/data-update
- 株価四本値: https://jpx-jquants.com/ja/spec/eq-bars-daily
- 調整済み株価の計算方法: https://jpx-jquants.com/ja/spec/eq-bars-daily/adj
- 上場銘柄一覧(マスタ): https://jpx-jquants.com/ja/spec/eq-master
- TOPIX四本値: https://jpx-jquants.com/ja/spec/idx-bars-daily-topix
- 配信対象指数コード: https://jpx-jquants.com/ja/spec/idx-bars-daily/indexcodes
- 日経225オプション四本値: https://jpx-jquants.com/ja/spec/drv-bars-daily-opt-225
- 業種別空売り比率: https://jpx-jquants.com/ja/spec/mkt-short-ratio
- 取引カレンダー: https://jpx-jquants.com/ja/spec/mkt-cal
- 財務省 国債金利情報: https://www.mof.go.jp/jgbs/reference/interest_rate/
  - 当年分CSV: https://www.mof.go.jp/jgbs/reference/interest_rate/jgbcm.csv
  - 過去分CSV: https://www.mof.go.jp/jgbs/reference/interest_rate/data/jgbcm_all.csv
  - 形式: Shift-JIS / `skiprows=1` / 基準日は和暦(S・H・R) / 欠測は `-` / 更新は翌営業日11:00頃

---

## 付録B 日本版(JFG)設計 — **凍結中。v2.0では実装しない**

費用の都合で凍結した設計。復活させる場合はこの付録を本文に戻す。**Claude Code はこの付録を実装しないこと。**

### B.1 復活の判断基準と手順
1. まず **J-Quants無料プラン(0円)** で P-JP1〜P-JP3 を実施する。無料プランは12週間遅れだが、**過去データでの設計と検証には支障がない**。効かないと分かれば1円も払わずに済む。
2. 検証に合格し、ライブ運用に移る時だけ **Lightプラン(月1650円)** を契約する。Standard(3300円)は不要。

### B.2 Lightプランで作る構成(7要素・等ウェイト)
Lightには全銘柄日足・銘柄マスタ・TOPIX四本値が含まれる。これがJFGの中核。欠ける3つは以下で代替する。

| # | 要素 | 定義 | Lightでの扱い |
|---|---|---|---|
| 1 | モメンタム | TOPIX ÷ 125日SMA − 1 | そのまま可 |
| 2 | 株価の強さ | プライム銘柄の(52週新高値−新安値)÷銘柄数 | そのまま可(全銘柄日足+マスタ) |
| 3 | 市場の幅 | McClellan Volume Summation | そのまま可 |
| 4 | プット/コール | 日経225オプションのP/C比 | **代替**: JPXが無料公表する「空売り集計(日次)」の空売り比率5日平均 |
| 5 | ボラティリティ | 日経VI ÷ 50日SMA | **代替**: TOPIXの実現ボラ20日 ÷ その50日SMA。急落初日の反応がやや鈍くなる点は許容 |
| 6 | 安全資産需要 | TOPIX20日リターン − 国債20日リターン | そのまま可(国債は財務省CSV) |
| 7 | リスク選好 | グロース250 − Core30 の20日リターン差 | **代替**: 全銘柄日足+マスタから自前バスケットを組む(グロース市場の等ウェイト vs 時価総額上位30) |

対象ユニバースは東証プライムの内国普通株。市場区分は**その日のマスタ**で判定する(生存バイアス回避)。

### B.3 正規化と合成
- 各要素の raw を 0–100 化。方式は候補 A(ローリング百分位)/ B(ローリングz値→標準正規CDF×100)/ C(ローリングmin-max)、窓 W ∈ {252, 504, 756, 1260} を総当たりし、**CNNの実データ(1.4章)を最もよく再現する方式を逆算して採用**する。特定できなければ既定を B(W=504) とする。
- 窓は過去方向のみ。窓が満たない期間はNaN。
- 反転要素(4,5)は `100 − score`。総合は有効要素の単純平均。有効6未満の日は総合を出さない。
- **重みの最適化は禁止**(極端局面の標本が少なく過学習になるため)。等ウェイト固定。

### B.4 検証(go/no-go)
- G1 先読みテスト: ランダム30日で「その日までのデータだけで再計算した値=保存値」
- G2 5区分の滞在日数比率
- G3 局面テスト: 2020-03、2024-08-05前後、2025-04-07前後で Extreme Fear になるか
- G4 先行リターン: score<25 突入後のTOPIX 5/10/20/60日リターン。標本を前半/後半に分け符号が一致するか
- G5 要素間相関(|ρ|>0.85 をフラグ)/ G6 窓±1段階での順位相関>0.9
- **結果が悪くても隠さず書く。パラメータを後から調整して見栄えを整えない。**

### B.5 運用
- 夕方17:10に暫定値(オプション系は前日持ち越し、`provisional:true`)、翌朝06:40に確定値。
- J-Quantsのデータは**再配布不可**。リポジトリはprivateにし、派生スコアのみ保存する。

---

---

## 付録C 斜線(トレンドライン)の検証記録 — 2026-09-18

CNNの実データで検証した結果。同じ検討を繰り返さないために残す。

**確認できた事実**
- ピボット判定を左右3本にすると、直近の小さな山が拾われず、線が1か月以上前の高値に引かれる。同じデータで抵抗線が52.0(遠い)にも23.7(すでに上抜け)にもなり、**どの高値を拾うかだけで結論が逆転する**。
- 左右1本に緩めると直近の高値を拾えるが、急落局面では傾きが1日−2.5ポイントに達し、延長すると数日で0を割る。→ 5.3.3の傾き上限の根拠。
- 高値3点と安値3点の線を同時に引くと、下降の勢いの差で**2本が交差**した(高値−2.46/日 対 安値−1.78/日)。交差後は「抵抗を上抜け」と「支持を下抜け」が同時に成立し、表示が破綻する。→ 5.3.3の交差時無効化の根拠。
- 3点に最小二乗を当てると、どの点も線を通らない(残差 −0.84/+1.67/−0.84)。「どこで抜けたか」の説明が難しくなる。

**水平線が優れている点**
- 確定したスイング安値・高値は事実なので、翌日以降も位置が変わらない(リペイントしない)。
- 水平線同士は交差しない。
- 「27.3を割ったか」の判定が一意。
- 必要本数が少ない(30本で足りる。斜線は57本必要だった)。
- 検証中、32.2(前の安値)と32.69(直近の戻り高値)がほぼ重なる節目が浮かび上がった。**これは水平線でしか見えない情報。**

**結論**: 水平線を既定とし、斜線は設定でONにしたときだけ描く。斜線を復活・強化する場合は、上の4点が解決できているかを先に確認すること。

<!-- KICKOFF-START -->
## 11. キックオフ指示文(Claude Code に最初に貼る)

このリポジトリの SPEC.md(FG ENGINE 仕様書 v2.2)を最初から最後まで読んでください。
冒頭の変更総括にある通り、v2.0 は米国版(CNN)のみの構成で、運用費0円が絶対条件です。付録Bの日本版は凍結中なので実装しないでください。
まず P1(CNN取得 → fg.json → GitHub Actions 稼働)だけを実施してください。
P1 では、CNNサイトの表示値と score の小数を3営業日ぶん突き合わせ、丸め規則が四捨五入か切り捨てかを判定して docs/rounding.md に記録してください。
P1 の検収条件の結果を一覧で報告し、私の確認を待ってから P2 に進んでください。
APIキーは不要です。課金が必要な外部サービスが必要だと判断した場合は、実装せずに理由と代替案を報告してください。
CNNの生データはコミットせず、スコアと派生統計のみを保存してください。
<!-- KICKOFF-END -->
